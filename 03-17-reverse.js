// day 5 - did this with Vonds during their review session

// Complete the solution so that it reverses the string passed into it.

const reverseStr = (string) => {
    return string.split('').reverse().join('')
}